/**
 * macOS Platform Adapters
 * 
 * This module provides macOS-specific implementations of platform adapters
 * for activity detection, notifications, and other OS-level features.
 */

export { ActivityDetector } from './activity-detector';

// Re-export types for convenience
export type { ActivityDetector as ActivityDetectorType } from './activity-detector';

